{
  "buildCommand": "cd genzura-web && npm install && npm run build",
  "outputDirectory": "genzura-web/dist",
  "framework": "vite",
  "installCommand": "cd genzura-web && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
